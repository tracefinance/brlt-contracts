# Vault0 Environment Configuration Example

# === General Configuration ===

# Path to a custom .env file (optional, defaults to .env in the current directory)
# ENV_FILE=/path/to/custom/.env

# Base directory for the application (defaults to current working directory if not set)
# APP_BASE_DIR=

# SQLite database file path (defaults to vault0.db in the project root)
DB_PATH=./vault0.db

# Server port to listen on (defaults to 8080)
SERVER_PORT=8080

# Path to static React UI files (defaults to ui/dist in the project root)
UI_PATH=./ui/dist

# Path to database migration files (defaults to migrations in the project root)
MIGRATIONS_PATH=./migrations

# Base64-encoded key for encrypting sensitive data in the database (REQUIRED - no default)
DB_ENCRYPTION_KEY=

# === Ethereum Configuration ===

# Ethereum RPC URL (defaults to public node)
ETHEREUM_RPC_URL=https://ethereum-rpc.publicnode.com

# Ethereum chain ID (defaults to 1 for mainnet)
ETHEREUM_CHAIN_ID=1

# Default gas price in Gwei for Ethereum transactions (defaults to 20)
ETHEREUM_GAS_PRICE=20

# Default gas limit for Ethereum transactions (defaults to 21000)
ETHEREUM_GAS_LIMIT=21000

# === Polygon Configuration ===

# Polygon RPC URL (defaults to polygon-rpc.com)
POLYGON_RPC_URL=https://polygon-rpc.com

# Polygon chain ID (defaults to 137 for mainnet)
POLYGON_CHAIN_ID=137

# Default gas price in Gwei for Polygon transactions (defaults to 30)
POLYGON_GAS_PRICE=30

# Default gas limit for Polygon transactions (defaults to 21000)
POLYGON_GAS_LIMIT=21000

# === Base Configuration ===

# Base RPC URL (defaults to mainnet.base.org)
BASE_RPC_URL=https://mainnet.base.org

# Base chain ID (defaults to 8453 for mainnet)
BASE_CHAIN_ID=8453

# Default gas price in Gwei for Base transactions (defaults to 10)
BASE_GAS_PRICE=10

# Default gas limit for Base transactions (defaults to 21000)
BASE_GAS_LIMIT=21000 